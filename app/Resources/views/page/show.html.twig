{% extends 'layout.html.twig' %}

{% block content %}
    {% if page %}
        <h1>{{ page.title }}</h1>   
        {{ page.body | purify }}
    {% else %}
        Brak zawartości
    {% endif %}
{% endblock %}
