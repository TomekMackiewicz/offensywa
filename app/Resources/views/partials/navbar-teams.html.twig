{% for team in teams %}
    <li>
        <a href="#" class="dropdown-toggle supmenu" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Rocznik {{ team.year }}
        </a>
        <ul class="submenu">
            <li><a href="{{ path('team_show', { 'year': team.year }) }}">Kadra</a></li> 
            <li><a href="{{ path('show_team_trainings', { 'year': team.year }) }}">Treningi</a></li>                       
        </ul>
    </li> 
{% endfor %}
